import { FilterSalaryNotPurePipe } from "./filter-salary-not-pure.pipe";

describe("FilterSalaryNotPurePipe", () => {
  it("create an instance", () => {
    const pipe = new FilterSalaryNotPurePipe();
    expect(pipe).toBeTruthy();
  });
});
